package com.paypal;

public enum TipConfigurationStatusType {
    /** Specifies the state when the tip configuration is activated and can be used to determine tip ping. */
    ACTIVE,

    /** Specifies the state when the tip configuration is paused. */
    INACTIVE
}
