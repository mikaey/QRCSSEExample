package com.paypal;

public enum PaymentInitiatorType {
    /** Indicates that the capture for payment is initiated by consumer. */
    CONSUMER,

    /** Indicates that the capture for payment is initiated by merchant. */
    MERCHANT
}
